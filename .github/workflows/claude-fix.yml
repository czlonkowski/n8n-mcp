name: Claude Code Fix

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  claude-fix:
    uses: 1215-Labs/.github/.github/workflows/claude-fix.yml@main
    secrets: inherit
